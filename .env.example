# Copy this file to `.env` and replace the placeholder values with your real secrets and URLs.
# This file is safe to commit. Your private `.env` file will be gitignored.

# Grafana admin credentials
GRAFANA_ADMIN_USER=admin
GRAFANA_ADMIN_PASSWORD=changeme

# Prometheus base URL (no trailing slash)
PROMETHEUS_URL=http://prometheus:9090

# Local Loki push API endpoint
LOCAL_LOKI_URL=http://localhost:3100/loki/api/v1/push

# Remote Loki push API endpoint
REMOTE_LOKI_URL=http://10.10.0.34:3100/loki/api/v1/push

# Remote node IPs for Prometheus scrape configs
REMOTE_NODE_IP_1=10.10.0.151
REMOTE_NODE_IP_2=10.10.0.152
